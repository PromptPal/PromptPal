#import * from './types/project.gql'

schema {
  query: Query
  mutation: Mutation
}

type Query {
  project(id: Int!): Project!
}

type Mutation {
  createProject(id: Int!): Project!
}
